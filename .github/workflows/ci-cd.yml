name: Capstone CI/CD

on:
  push:
    branches:
      - main

jobs:

  build-and-test:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout
        uses: actions/checkout@v4

      # TODO
      # Configurare Python

      # TODO
      # Installare le dipendenze

      # TODO
      # Eseguire i test

      # TODO
      # Eseguire Bandit

      # TODO
      # Eseguire pip-audit

  deploy:

    needs: build-and-test

    runs-on: ubuntu-latest

    steps:

      # TODO
      # Deploy Azure