# fastapi: Il framework web principale. È asincrono (async), estremamente veloce e 
# genera automaticamente la documentazione interattiva Swagger per testare gli endpoint AI.
fastapi==0.110.0

# uvicorn: Il server web di produzione (ASGI) che si occupa di far girare l'applicazione 
# FastAPI, gestendo le connessioni HTTP in ingresso e instradandole verso il codice Python.
uvicorn==0.28.0

# transformers: La libreria di Hugging Face che permette di scaricare, inizializzare 
# e utilizzare modelli di Intelligenza Artificiale e Deep Learning pre-addestrati (come DistilBERT).
transformers==4.38.2

# torch: Il motore di calcolo (PyTorch) richiesto sotto il cofano dalla libreria transformers 
# per eseguire i calcoli matematici del modello. Viene forzata la versione CPU (--index-url) 
# per evitare di scaricare i pacchetti GPU (CUDA), riducendo il peso del container da 5GB a circa 300MB.
torch==2.2.1 --index-url https://download.pytorch.org/whl/cpu

# azure-monitor-opentelemetry: L'SDK ufficiale di Microsoft basato sullo standard OpenTelemetry. 
# Serve per agganciare l'applicazione ad Azure Application Insights, permettendo l'invio automatico 
# di metriche (latenza), log di errore e tracciamento delle richieste HTTP (Modulo 4).
azure-monitor-opentelemetry==1.6.0